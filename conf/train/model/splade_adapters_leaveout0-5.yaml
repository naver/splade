# @package _global_

init_dict:
  model_type_or_dir: distilbert-base-uncased 
  model_type_or_dir_q: null
  freeze_d_model: 0
  agg: max
  fp16: true
  adapter_config: houlsby
  adapter_name: houlsby 
  leave_out: 0 1 2 3 4 5

config:
  tokenizer_type: distilbert-base-uncased
